{
  "name": "jsbook",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "resinstall": "lerna bootstrap",
    "reinstall:legacy": "lerna bootstrap -- --legacy-peer-deps"
  }
}
